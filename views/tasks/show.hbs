<head>
    <style>
.taskTitle {
  color: rgb(131, 243, 228);
 
} 
.taskName {
  color: rgb(219, 252, 143);
  text-align: center;
  font-weight: bold;
     
 
} 
</style>
</head>


<h3 class="taskTitle">Here's your task</h3>
<div class="row" >
    <form action="/tasks/{{task._id}}" method="GET" class="col s12" >
        <div class="row">
            <div class="input-field"  class="taskTitle">
                <input type="text" name="title" id="title" value="{{task.title}}"  class="taskName">
                <label for="title">Title</label>
            </div>
        </div>
        
        <div class="row">
            <div class="input-field">
                <h5  class="taskTitle">Individual tasks</h5><p>(each task is in subsequent lines)</p>
                <textarea id="body" name="body">{{task.body}}</textarea>
            </div>
        </div>

        <div class="row" >
            <a href="/dashboard" class="btn orange">Back to dashboard</a>
        </div>
    </form>
</div>